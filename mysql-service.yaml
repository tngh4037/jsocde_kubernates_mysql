apiVersion: v1
kind: Service

metadata:
  name: mysql-service

spec:
  type: NodePort
  selector:
    app: mysql-db # 어떤 Pod 와 연결할지
  ports:
    - protocol: TCP
      nodePort: 30002
      port: 3306
      targetPort: 3306